import UserGiftCatalog from "@/components/gifts/UserGiftCatalog";
import AppLayout from "@/components/layouts/AppLayout";

const GiftCatalog = () => {
  return (
    <AppLayout>
      <UserGiftCatalog />
    </AppLayout>
  );
};

export default GiftCatalog;
